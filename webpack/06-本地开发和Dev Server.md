
理想的本地开发环境：

```

1. 支持 http 服务运行，即浏览器输入 url 可以进行访问，而不是以文件形式在浏览器打开

一来更接近生产环境，二来可以避免网络请求等API会存在跨域问题


2. 修改完代码可以自动重新构建，并在浏览器实时显示最新的运行结果

可以让开发更专注，不用频繁手动构建和点击浏览器刷新按钮


3. 支持 Source Map

可以在运行过程中出现错误时快速定位，而不是调试打包后的结果，因为打包后的结果往往不可阅读


```
